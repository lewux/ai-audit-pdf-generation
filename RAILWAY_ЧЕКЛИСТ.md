# Чеклист настройки Railway

## ✅ Обязательные переменные окружения

Добавьте в Railway Dashboard → Variables:

```
NODE_ENV=production
PORT=3001
JWT_SECRET=your-super-secret-jwt-key-here-min-32-chars-change-this-in-production
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
```

## ⚙️ Настройки сервиса

### Build & Deploy
- **Build Command**: Оставьте пустым
- **Start Command**: Оставьте пустым

### Resources
- **Memory**: 1GB (минимум 512MB)
- **CPU**: Стандартный план

### Health Check
- **Path**: `/api/health`
- **Timeout**: 30 секунд

## 🔍 Проверка после деплоя

1. **Проверьте логи:**
   - Должно быть: `✅ PDF Server running on http://0.0.0.0:PORT`
   - Не должно быть ошибок про JWT_SECRET

2. **Проверьте health endpoint:**
   ```bash
   curl https://your-app.railway.app/api/health
   ```

3. **Проверьте генерацию PDF:**
   - Получите токен через `/api/auth/token`
   - Сгенерируйте PDF через `/api/pdf`
   - Проверьте, что файл доступен

## ⚠️ Важные замечания

### Файлы не сохраняются
- Railway использует эфемерную файловую систему
- PDF файлы теряются при редеплое
- **Решение**: Используйте Railway Volumes или внешнее хранилище (S3)

### Память
- Puppeteer требует много памяти
- Мониторьте использование в Railway Metrics
- При ошибках "Out of memory" увеличьте Memory до 2GB

### CORS
- Обязательно установите `ALLOWED_ORIGINS`
- Без этого запросы с фронтенда будут блокироваться

## 🐛 Решение проблем

### Сервер не запускается
- Проверьте логи на наличие ошибок
- Убедитесь, что `JWT_SECRET` установлен
- Проверьте, что Memory >= 512MB

### PDF не генерируется
- Проверьте логи Puppeteer
- Убедитесь, что Memory >= 1GB
- Проверьте, что все assets скопированы

### Файлы не доступны
- Помните: файлы теряются при редеплое
- Используйте Railway Volumes для постоянного хранения
- Или настройте внешнее хранилище

