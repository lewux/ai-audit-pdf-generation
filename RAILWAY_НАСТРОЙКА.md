# Быстрая настройка Railway

## ✅ Что уже сделано

Проект оптимизирован для Railway:
- ✅ Создан `.railwayignore` - исключает ненужные файлы
- ✅ Оптимизирован `.npmrc` - быстрая установка зависимостей
- ✅ Добавлен `railway.json` - настройки билда
- ✅ Оптимизирован Puppeteer для Railway
- ✅ Добавлен build скрипт в `package.json`

## 🚀 Настройки в Railway Dashboard

### 1. Переменные окружения (Environment Variables)

В настройках вашего сервиса в Railway добавьте:

```
NODE_ENV=production
PORT=3001
PUPPETEER_CACHE_DIR=/tmp/.cache/puppeteer
```

**Где найти:**
1. Откройте ваш проект в Railway
2. Выберите сервис (Service)
3. Перейдите в "Variables"
4. Добавьте переменные выше

### 2. Настройки Build & Deploy

**Build Command:** Оставьте пустым (Railway использует `railway.json`)

**Start Command:** Оставьте пустым (Railway использует `railway.json`)

**Где найти:**
1. Выберите сервис
2. Перейдите в "Settings"
3. Раздел "Build & Deploy"
4. Оставьте поля пустыми

### 3. Ресурсы (Resources)

**Рекомендуемые настройки:**
- **Memory**: 1GB (минимум 512MB для Puppeteer)
- **CPU**: Стандартный план

**Где найти:**
1. Выберите сервис
2. Перейдите в "Settings"
3. Раздел "Resources"
4. Установите Memory на 1GB

### 4. Автоматический деплой

**Настройки:**
- ✅ Auto Deploy: Включено
- Branch: `main` (или ваша основная ветка)

**Где найти:**
1. Выберите сервис
2. Перейдите в "Settings"
3. Раздел "Deploy"
4. Включите "Auto Deploy"

## ⚡ Ожидаемое время билда

- **Первый билд**: 3-5 минут (скачивание Chromium)
- **Последующие билды**: 1-2 минуты (с кэшем)

## 🔍 Проверка оптимизации

После настройки проверьте:

1. **Время билда** в Railway Dashboard → Deployments
2. **Использование памяти** в Metrics
3. **Логи** на наличие ошибок

## 📝 Важные замечания

1. **package-lock.json** должен быть закоммичен в Git
2. **.railwayignore** исключает ненужные файлы из деплоя
3. Railway автоматически кэширует `node_modules` между билдами
4. Chromium кэшируется в `/tmp/.cache/puppeteer`

## 🐛 Решение проблем

### Билд долгий (>5 минут)
- Проверьте размер репозитория (должен быть < 100MB)
- Убедитесь, что `.railwayignore` работает
- Проверьте логи билда в Railway

### Ошибки Puppeteer
- Увеличьте Memory до 1GB
- Проверьте переменную `PUPPETEER_CACHE_DIR`

### Ошибки установки зависимостей
- Убедитесь, что `package-lock.json` в репозитории
- Проверьте версию Node.js (>= 18.0.0)

## 📚 Дополнительная информация

Подробная документация: `RAILWAY_SETUP.md`
