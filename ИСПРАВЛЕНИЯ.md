# Исправления для Railway

## ✅ Что было исправлено

### 1. **КРИТИЧНО: HOST изменен с localhost на 0.0.0.0**
   - **Было**: `const HOST = process.env.HOST || 'localhost';`
   - **Стало**: `const HOST = process.env.HOST || '0.0.0.0';`
   - **Почему**: Railway требует слушать на 0.0.0.0 для доступа извне

### 2. **Добавлена валидация переменных окружения**
   - Проверка обязательных переменных при старте
   - Предупреждения о недостающих переменных
   - Сервер не запустится без JWT_SECRET в production

### 3. **Оптимизированы пути к директориям**
   - Использование абсолютных путей вместо относительных
   - Более надежная работа на Railway

### 4. **Оптимизировано копирование assets**
   - Assets копируются только если их еще нет
   - Ускорение последующих генераций PDF

### 5. **Улучшено логирование**
   - Более информативные сообщения при старте
   - Предупреждения о проблемах конфигурации

## 📋 Что нужно сделать в Railway

### Обязательные переменные:
```
NODE_ENV=production
JWT_SECRET=your-super-secret-jwt-key-here-min-32-chars
ALLOWED_ORIGINS=https://yourdomain.com
```

### Настройки:
- Memory: 1GB
- Health Check: `/api/health`

## ⚠️ Остающиеся проблемы (требуют вашего внимания)

### 1. **Эфемерная файловая система**
   - PDF файлы теряются при редеплое
   - **Решение**: Используйте Railway Volumes или внешнее хранилище

### 2. **CORS настройки**
   - Обязательно установите `ALLOWED_ORIGINS`
   - Без этого запросы с фронтенда будут блокироваться

### 3. **Память для Puppeteer**
   - Требуется минимум 512MB, рекомендуется 1GB
   - Мониторьте использование в Railway Metrics

## 📚 Документация

- `RAILWAY_ПРОБЛЕМЫ.md` - подробное описание всех проблем
- `RAILWAY_ЧЕКЛИСТ.md` - пошаговый чеклист настройки
- `RAILWAY_НАСТРОЙКА.md` - быстрая инструкция
- `RAILWAY_SETUP.md` - подробная документация
