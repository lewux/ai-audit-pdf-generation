# Потенциальные проблемы при деплое на Railway

## 🔴 Критические проблемы (могут сломать сервис)

### 1. **HOST = 'localhost' - СЕРЬЕЗНАЯ ПРОБЛЕМА!**

**Проблема:**
```javascript
const HOST = process.env.HOST || 'localhost';  // ❌ НЕ РАБОТАЕТ НА RAILWAY!
```

**Почему это проблема:**
- Railway назначает динамический порт через переменную `PORT`
- Сервер должен слушать на `0.0.0.0`, а не `localhost`
- На `localhost` сервер будет недоступен извне

**Решение:**
```javascript
const HOST = process.env.HOST || '0.0.0.0';  // ✅ Правильно для Railway
```

### 2. **Отсутствие обязательных переменных окружения**

**Проблема:**
- `JWT_SECRET` - обязателен для аутентификации
- Если не установлен, сервер упадет при попытке генерации токена

**Решение:**
Добавить в Railway Variables:
```
JWT_SECRET=your-super-secret-jwt-key-here-min-32-chars-change-this-in-production
```

### 3. **Эфемерная файловая система Railway**

**Проблема:**
- Railway использует эфемерную файловую систему
- Файлы в `/uploads` теряются при перезапуске/редеплое
- PDF файлы не сохраняются между деплоями

**Решение:**
- Использовать внешнее хранилище (S3, Cloudflare R2, Railway Volumes)
- Или генерировать PDF "на лету" без сохранения
- Или использовать временное хранилище с коротким TTL

### 4. **CORS настройки**

**Проблема:**
```javascript
origin: process.env.ALLOWED_ORIGINS ? 
    process.env.ALLOWED_ORIGINS.split(',') : 
    ['http://localhost:3000'],  // ❌ Не работает в продакшене
```

**Решение:**
Обязательно установить в Railway:
```
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
```

## ⚠️ Важные проблемы (могут вызвать ошибки)

### 5. **Puppeteer и память**

**Проблема:**
- Puppeteer требует много памяти (минимум 512MB, лучше 1GB)
- Может упасть с ошибкой "Out of memory"
- Chromium занимает ~300MB памяти

**Решение:**
- Установить Memory в Railway на 1GB
- Использовать `--single-process` (уже добавлено)
- Мониторить использование памяти

### 6. **Таймауты Puppeteer**

**Проблема:**
```javascript
await page.waitForTimeout(3000);  // Фиксированные таймауты
await page.waitForTimeout(2000);
```

**Почему проблема:**
- На медленных серверах может не хватить времени
- На быстрых - лишнее ожидание

**Решение:**
- Использовать `waitForSelector` вместо `waitForTimeout`
- Или сделать таймауты настраиваемыми через env

### 7. **file:// протокол для загрузки HTML**

**Проблема:**
```javascript
await page.goto(`file://${htmlPath}`, {
    waitUntil: ['networkidle0', 'domcontentloaded']
});
```

**Почему может быть проблема:**
- На некоторых системах file:// может иметь ограничения
- Лучше использовать HTTP сервер или data: URL

**Решение:**
- Использовать встроенный HTTP сервер для раздачи файлов
- Или использовать `data:` URL для HTML

### 8. **Относительные пути**

**Проблема:**
```javascript
await fs.ensureDir('./uploads');  // Относительный путь
await fs.ensureDir('./logs');
```

**Почему проблема:**
- Рабочая директория может быть разной
- Лучше использовать абсолютные пути

**Решение:**
- Использовать `path.join(__dirname, 'uploads')` (уже используется в PDFGenerator)
- Но в server.js используется относительный путь

### 9. **Копирование assets при каждой генерации**

**Проблема:**
```javascript
await this.copyAssets();  // Копирует assets каждый раз
```

**Почему проблема:**
- Медленно и неэффективно
- На Railway это может быть особенно медленно

**Решение:**
- Копировать assets один раз при старте сервера
- Или использовать абсолютные пути к assets

### 10. **Логи в файлы**

**Проблема:**
```javascript
await fs.ensureDir('./logs');  // Создает директорию для логов
```

**Почему проблема:**
- На Railway логи лучше писать в stdout/stderr
- Файловые логи теряются при редеплое

**Решение:**
- Использовать только console.log (Railway автоматически собирает логи)
- Или настроить Winston для вывода в stdout

## 💡 Рекомендации по оптимизации

### 11. **Health check endpoint**

**Хорошо:**
- У вас есть `/api/health` endpoint ✅
- Railway может использовать его для проверки работоспособности

**Рекомендация:**
Настроить Health Check в Railway на `/api/health`

### 12. **Graceful shutdown**

**Хорошо:**
- У вас есть обработчики SIGTERM и SIGINT ✅
- Railway отправляет SIGTERM при остановке

### 13. **Rate limiting**

**Хорошо:**
- Rate limiting настроен ✅
- Защита от DDoS

**Рекомендация:**
Настроить через env переменную `API_RATE_LIMIT` если нужно

## 📋 Чеклист перед деплоем

- [ ] Изменить `HOST` на `0.0.0.0`
- [ ] Установить `JWT_SECRET` в Railway
- [ ] Установить `ALLOWED_ORIGINS` в Railway
- [ ] Установить Memory на 1GB
- [ ] Проверить все переменные окружения
- [ ] Настроить Health Check
- [ ] Решить проблему с хранением файлов (Volumes или внешнее хранилище)
- [ ] Протестировать генерацию PDF после деплоя

